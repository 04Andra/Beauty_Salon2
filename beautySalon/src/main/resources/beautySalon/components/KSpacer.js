import {StyleSheet, View} from 'react-native';

import React from "react";

export default function KSpacer({height = 50}) {
    return (
        <View style={{height: height}}>

        </View>
    );
}

const KSpacerStyles = StyleSheet.create({

});
